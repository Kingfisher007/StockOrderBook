<UserControl x:Class="EOrderBook.View.Controls.Simulation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EOrderBook.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition  Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" HorizontalAlignment="Center" Margin="5,5,5,10"  VerticalAlignment="Center" Content="{Binding Status}" />
        <Button Grid.Row="1" HorizontalAlignment="Center" Margin="5,10,5,5"  VerticalAlignment="Center" Command="{Binding Simulation}">Simulate</Button>
    </Grid>
</UserControl>
